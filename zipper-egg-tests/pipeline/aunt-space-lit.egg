; AUTO-GENERATED pipeline stage 1 (aunt) — instance sides exceed the eager egg
; budget; structural equivalence is carried by the Scala executor gates and the
; agnostic certificates.  This file verifies the reference output's observations.
(include "bridge-prelude.egg")
(let $want (Reflect (TNode (F) (CC 152 (TNode (F) (CC 7 (TNode (F) (CC 40 (TNode (T) (CNil)) (CNil))) (CC 34 (TNode (F) (CC 7 (TNode (T) (CNil)) (CNil))) (CC 35 (TNode (F) (CC 40 (TNode (T) (CNil)) (CNil))) (CNil))))) (CNil)))))
(let $ma0 (Term (Sub 7 (Sub 34 (Sub 152 $want)))))
(let $ma1 (Term (Sub 40 (Sub 35 (Sub 152 $want)))))
(let $ma2 (Term (Sub 40 (Sub 7 (Sub 152 $want)))))
(let $na0 (Term (Sub 0 $want)))
(let $na1 (Term (Sub 2 $want)))
(let $na2 (Term (Sub 5 $want)))
(let $na3 (Term (Sub 6 $want)))
(let $na4 (Term (Sub 7 $want)))
(let $na5 (Term (Sub 10 $want)))
(let $na6 (Term (Sub 19 $want)))
(let $na7 (Term (Sub 32 $want)))
(let $na8 (Term (Sub 34 $want)))
(let $na9 (Term (Sub 35 $want)))
(let $na10 (Term (Sub 40 $want)))
(let $na11 (Term (Sub 48 $want)))
(let $na12 (Term (Sub 0 (Sub 152 $want))))
(let $na13 (Term (Sub 2 (Sub 152 $want))))
(let $na14 (Term (Sub 5 (Sub 152 $want))))
(let $na15 (Term (Sub 6 (Sub 152 $want))))
(let $na16 (Term (Sub 10 (Sub 152 $want))))
(let $na17 (Term (Sub 19 (Sub 152 $want))))
(let $na18 (Term (Sub 32 (Sub 152 $want))))
(let $na19 (Term (Sub 40 (Sub 152 $want))))
(let $na20 (Term (Sub 48 (Sub 152 $want))))
(let $na21 (Term (Sub 152 (Sub 152 $want))))
(let $na22 (Term (Sub 0 (Sub 7 (Sub 152 $want)))))
(let $na23 (Term (Sub 2 (Sub 7 (Sub 152 $want)))))
(let $na24 (Term (Sub 5 (Sub 7 (Sub 152 $want)))))
(let $ra (Term $want))

(run 120)

(check (= $ra (F)))
(check (= $ma0 (T)))
(check (= $ma1 (T)))
(check (= $ma2 (T)))
(check (= $na0 (F)))
(check (= $na1 (F)))
(check (= $na2 (F)))
(check (= $na3 (F)))
(check (= $na4 (F)))
(check (= $na5 (F)))
(check (= $na6 (F)))
(check (= $na7 (F)))
(check (= $na8 (F)))
(check (= $na9 (F)))
(check (= $na10 (F)))
(check (= $na11 (F)))
(check (= $na12 (F)))
(check (= $na13 (F)))
(check (= $na14 (F)))
(check (= $na15 (F)))
(check (= $na16 (F)))
(check (= $na17 (F)))
(check (= $na18 (F)))
(check (= $na19 (F)))
(check (= $na20 (F)))
(check (= $na21 (F)))
(check (= $na22 (F)))
(check (= $na23 (F)))
(check (= $na24 (F)))
