; AUTO-GENERATED — op-intersection: recursive trie implementation in egg = Scala
; egglog RUNS the modelled recursive set-operation implementation on the transpiled zipper
; ($impl) and checks it reduces to exactly the trie the Scala implementation computed ($want).

(include "impl-prelude.egg")
(let $impl (TrI (Node (F) (CC 166 (Node (T) (CNil)) (CC 167 (Node (T) (CNil)) (CNil)))) (Node (F) (CC 167 (Node (T) (CNil)) (CC 169 (Node (T) (CNil)) (CNil))))))
(let $want (Node (F) (CC 167 (Node (T) (CNil)) (CNil))))

(run 400)

(check (= $impl $want))
