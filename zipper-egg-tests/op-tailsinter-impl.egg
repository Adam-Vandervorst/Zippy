; AUTO-GENERATED — op-tailsinter: recursive trie implementation in egg = Scala
; egglog RUNS the modelled recursive set-operation implementation on the transpiled zipper
; ($impl) and checks it reduces to exactly the trie the Scala implementation computed ($want).

(include "impl-prelude.egg")
(let $impl (TrTI (Node (F) (CC 12305 (Node (F) (CC 12303 (Node (T) (CNil)) (CNil))) (CC 12309 (Node (F) (CC 12303 (Node (T) (CNil)) (CNil))) (CNil))))))
(let $want (Node (F) (CC 12303 (Node (T) (CNil)) (CNil))))

(run 400)

(check (= $impl $want))
