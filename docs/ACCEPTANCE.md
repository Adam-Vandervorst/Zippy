# Acceptance status

<!-- GENERATED by scripts/gen_acceptance.py; do not edit — `python3 scripts/gen_acceptance.py` regenerates, `--check` fails on drift -->

Snapshot `2489c08c0794` — **the working tree is dirty**: rows whose evidence is uncommitted are UNMEASURED. Gate results from `target/gates.tsv` (`scripts/gates.py --run`); closures from `target/trace-closure.tsv` (`scripts/proof_closure.py --check`).

Every row is derived: the acceptance sentence from tasks.md, the gates and closures from docs/ACCEPTANCE_MAP.tsv, their results from the recorded runs. States: **GREEN** (every gate passed, every closure discharged), **CONDITIONAL** (gates passed; a named closure is OPEN or rests on a trusted-base entry), **UNSOUND** (a soundness gate failed), **SOUND-BUT-NOT-USEFUL** (only a usefulness gate failed), **UNMEASURED** (a gate has no result or the evidence on disk is not the committed one).

| item | state | why | gates | closure |
|---|---|---|---|---|
| **A1** Specify counted executions and resource events | **GREEN** | — | ✓ morkl.SpatialSemanticsCheck<br>✓ morkl.SpatialEventsCheck | - |
| **A2** Introduce the stratified delta-fixpoint IR | **GREEN** | — | ✓ morkl.DeltaIRCheck<br>✓ mechanized theorems build (0.5) | - |
| **A3** Replace independent scalar widening with correlated resource facts | **GREEN** | — | ✓ morkl.SpatialDomainCheck | - |
| **A4** Derive resource bounds by abstract interpretation of the event semantics | **GREEN** | — | ✓ morkl.SpatialCostCheck<br>✓ morkl.SpatialEventsCheck<br>✓ morkl.SpatialScaleCheck<br>✓ morkl.SpatialPipelineCheck | - |
| **A5** Make calls and recursive components compositional in the resource domain | **GREEN** | — | ✓ morkl.CrossFunctionCostCheck | - |
| **A6** Prove or check every abstract transfer | **CONDITIONAL** | modulo spatial rule A6-BACKENDS: PROVED-MODULO T9; spatial rule A6-EVENTS: PROVED-MODULO T9; spatial rule A6-SUMM: PROVED-MODULO T9 | ✓ proof status vs the trusted base, one dependency graph over the traces (item 8, C4)<br>✓ mechanized theorems build (0.5) | spatial |
| **B1** Represent residual alternatives explicitly | **GREEN** | — | ✓ morkl.AlternativesCheck | - |
| **B2** Select from a certified Pareto frontier | **UNMEASURED** | evidence not the committed snapshot: proofs/decisions | ✓ morkl.ParetoCheck<br>✓ selection certificates replay independently (B2, B3, D2) | - |
| **B3** Demonstrate decisions that existing optimizers cannot make safely | **UNMEASURED** | evidence not the committed snapshot: proofs/decisions, docs/DECISIONS.md | ✓ morkl.DecisionsCheck<br>✓ selection certificates replay independently (B2, B3, D2) | - |
| **C1** Prove semantic substitution for the production operation | **GREEN** | — | ✓ mechanized theorems build (0.5) | O6a |
| **C2** Discharge unfolding and repeated folding | **GREEN** | — | ✓ mechanized theorems build (0.5) | O12a;O12b |
| **C3** Make proof traces typed, compositional objects | **UNMEASURED** | evidence not the committed snapshot: proofs/pipeline/traces | ✓ morkl.ProofTraceCheck<br>✓ typed proof traces: every declared cell resolves to a checked DAG (C3) | - |
| **C4** Enforce transitive trust closure | **UNMEASURED** | evidence not the committed snapshot: proofs/pipeline/traces | ✓ proof status vs the trusted base, one dependency graph over the traces (item 8, C4)<br>✓ trust closure mutation: an injected open O6a reaches its consumers (C4) | traces |
| **D1** Replace marker coverage with structural coverage | **UNMEASURED** | evidence not the committed snapshot: proofs/pipeline/COVERAGE.tsv | ✓ structural coverage: every feature inside a checked chain, census complete (D1)<br>✓ structural coverage mutations are caught (D1)<br>✓ pipeline marker/declaration audit (item 4) | traces |
| **D2** Rebuild every cornerstone claim independently | **UNMEASURED** | evidence not the committed snapshot: proofs/pipeline/resources, proofs/pipeline/RESOURCES.tsv | ✓ morkl.EquivPipelineTest<br>✓ cornerstone resource certificates contain their counted runs; selections replay (D2)<br>✓ pipeline claims accepted: no SINGLE-SIDE, BUDGET or chained cell (D2) | traces |
| **D3** Make puzzle15 a first-class stress theorem | **UNMEASURED** | evidence not the committed snapshot: proofs/puzzle15 | ✗ morkl.Puzzle15Check | - |
| **E1** Turn ambitious properties into discriminating gates | **GREEN** | — | ✓ morkl.MutationGates<br>✓ structural coverage mutations are caught (D1)<br>✓ trust closure mutation: an injected open O6a reaches its consumers (C4) | - |
| **E2** Publish atomically from the accepted commit | **UNMEASURED** | no recorded result for: publication reproduces from the accepted commit (E2) | ? publication reproduces from the accepted commit (E2) | - |
| **E3** Derive repository status from evidence | **UNMEASURED** | evidence not the committed snapshot: docs/ACCEPTANCE.md, docs/acceptance.tsv | ✓ acceptance status agrees with the evidence (E3) | - |

## Acceptance sentences

### A1. Specify counted executions and resource events — GREEN

differential tests show that instrumented executions and `SpatialEvents` produce the same event multiset for every constructor and backend; the test corpus includes empty, singleton, aliased, disjoint, deeply shared, recursive, and n-ary cases.

### A2. Introduce the stratified delta-fixpoint IR — GREEN

positive mutual recursion and negative dependencies on lower strata are accepted; cycles containing a negative or unknown edge are rejected; naive and delta execution produce the same accumulator at every round boundary and the same stationary result; recursive `Call` SCCs and explicit `Fixpoint` lower to the same IR when extensionally equivalent; every accepted IR instance carries replayable Lean-backed stratification and delta premises.

### A3. Replace independent scalar widening with correlated resource facts — GREEN

lattice and gamma checks pass; exact-tier disjointness and extrema survive depth and width collapse; summarized results contain all counted outcomes; results are invariant under prior analyses in the same JVM; every loss of precision is present in the returned analysis certificate.

### A4. Derive resource bounds by abstract interpretation of the event semantics — GREEN

zero containment failures under exhaustive small-model checks and randomized soundness hunts; the two known zipper counterexamples are covered as permanent regressions; all four spatial cost suites pass without workload-specific exceptions or stale expected formulae.

### A5. Make calls and recursive components compositional in the resource domain — GREEN

cross-function tests include changing path arguments, mutually recursive routines, summary reuse, range selection of call results, and calls below binders; composed bounds contain counted executions and remain materially tighter than `top` on the cornerstone call chains.

### A6. Prove or check every abstract transfer — CONDITIONAL

all transfer rules are either Lean-checked or accepted by the independent checker under a documented universal argument; `proof_closure.py` rejects a certified cost result if any transfer dependency is open.

Evidence: [`proofs/spatial/STATUS.tsv`](proofs/spatial/STATUS.tsv)

### B1. Represent residual alternatives explicitly — GREEN

a deterministic test fixture exposes at least three non-equivalent-cost but semantically equivalent residual choices, with no ground evaluation used to create or merge them.

### B2. Select from a certified Pareto frontier — UNMEASURED

selection is deterministic; every removal is replayable by an independent checker; changing objectives selects the expected alternatives in fusion, prefix/range, call-composition, sharing, and recursion fixtures.

Evidence: [`proofs/decisions`](proofs/decisions)

### B3. Demonstrate decisions that existing optimizers cannot make safely — UNMEASURED

every selected alternative is semantically certified, its counted resources lie in the predicted intervals, and at least one case per transformation family requires correlation or call composition unavailable to a scalar cardinality estimator.

Evidence: [`proofs/decisions`](proofs/decisions), [`docs/DECISIONS.md`](docs/DECISIONS.md)

### C1. Prove semantic substitution for the production operation — GREEN

O6a is `MECHANIZED`, all substitution traces replay, capture regressions pass, and no production call site implements independent substitution logic.

### C2. Discharge unfolding and repeated folding — GREEN

arbitrary positive-fragment transformation traces replay without open premises; O12a and O12b are mechanized; no bounded-depth experiment is cited as theorem evidence.

### C3. Make proof traces typed, compositional objects — UNMEASURED

every accepted pipeline claim resolves to a finite trace DAG whose leaves are checked universal theorems or direct backend obligations; mutation tests of a matcher, side condition, before/after term, or dependency make replay fail.

Evidence: [`proofs/pipeline/traces`](proofs/pipeline/traces)

### C4. Enforce transitive trust closure — UNMEASURED

injecting an open O6a-like dependency changes every transitive consumer to conditional or failed; removing it restores the status; no unqualified `PROVED` or `CERTIFIED-COST` result has an open leaf.

Evidence: [`proofs/pipeline/traces`](proofs/pipeline/traces)

### D1. Replace marker coverage with structural coverage — UNMEASURED

empty items and unattached strings are rejected; deleting any relevant AST or trace node removes its coverage; no accepted cell depends on an open registry entry.

Evidence: [`proofs/pipeline/COVERAGE.tsv`](proofs/pipeline/COVERAGE.tsv)

### D2. Rebuild every cornerstone claim independently — UNMEASURED

every declared cell is independently constructed and has a replayable semantic trace; every claimed resource result contains its counted run; there are no open, budget, chained, or single-side acceptance cells.

Evidence: [`proofs/pipeline/resources`](proofs/pipeline/resources), [`proofs/pipeline/RESOURCES.tsv`](proofs/pipeline/RESOURCES.tsv), [`proofs/pipeline/CLAIMS.tsv`](proofs/pipeline/CLAIMS.tsv)

### D3. Make puzzle15 a first-class stress theorem — UNMEASURED

no bound exceeds the finite maximum permitted by the proved state space; counted resource use is contained; intervals satisfy the committed usefulness thresholds; at least one backend/transformation choice is justified by the certificate rather than benchmark fitting.

Evidence: [`proofs/puzzle15`](proofs/puzzle15)

### E1. Turn ambitious properties into discriminating gates — GREEN

each major requirement has at least one mutation that fails only when that property is weakened; all gates pass twice with byte-identical counted and certificate output on a clean remote snapshot.

### E2. Publish atomically from the accepted commit — UNMEASURED

`publish_benchmarks.py --reproduce` succeeds from the clean accepted commit and no artifact names a dirty or different tree.

### E3. Derive repository status from evidence — UNMEASURED

deliberately opening a theorem, widening a cornerstone beyond its usefulness limit, or dirtying a generated result changes the corresponding row away from `GREEN`.

Evidence: [`docs/ACCEPTANCE.md`](docs/ACCEPTANCE.md), [`docs/acceptance.tsv`](docs/acceptance.tsv)

